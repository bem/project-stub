block('test2').content()(function() {
    return this.doAsync(function() {
        var defer = Vow.defer(),
            promise = defer.promise();

        setTimeout(function() {
            defer.resolve(applyCtx({ block: 'test3' }));
        }, 10);

        return promise;
    });
});
